# Secrets — API keys live here and must NEVER be committed
.env
.env.local

# Dependencies
node_modules/

# Logs
*.log

# Bluetooth captures (contain device addresses; RE artifacts, not for the repo)
captures/
*.zip

# OS cruft
.DS_Store

# Interceptor verification screenshots (local browser-testing artifacts)
interceptor-screenshot-*.png
